#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for uppercase
 * @c: character to be checked for case
 * Return: 1 if uppercase, 0 if not
 */

int _isupper(int c)
{
	if   (c>='A' && c <= 'Z')
		return (1);
	retirn (0);
}
